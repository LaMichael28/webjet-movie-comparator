# Rename this file to `.env` and fill in the required values
# Never commit the actual `.env` file with secrets to Git!

# ==== Backend Environment Variables ====
WEBJET_API_TOKEN=your-api-token-here          # Replace with the API token
ASPNETCORE_URLS=http://+:5190                 # Leave as-is unless port changes